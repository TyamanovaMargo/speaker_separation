# Core dependencies
numpy>=1.24.0
scipy>=1.10.0
librosa>=0.10.0
soundfile>=0.12.0
tqdm>=4.65.0

# PyTorch (install separately based on CUDA version)
# See install.sh for automatic installation
torch>=2.0.0
torchaudio>=2.0.0

# TensorRT (optional, for 2-3x speedup)
# torch-tensorrt>=1.4.0

# ModelScope for MossFormer2
modelscope>=1.11.0
